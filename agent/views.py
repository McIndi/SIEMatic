"""
Views for the agent app.
Define HTTP endpoints and rendering logic for agent-related pages.
"""

import logging
logger = logging.getLogger(__name__)
logger.debug("agent.views module loaded.")

from django.shortcuts import render

# Create your views here.
