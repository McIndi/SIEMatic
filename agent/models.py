"""
Models for the agent app.
Define database schema for agent-related data.
"""

import logging
logger = logging.getLogger(__name__)
logger.debug("agent.models module loaded.")

from django.db import models

# Create your models here.
